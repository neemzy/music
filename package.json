{
  "scripts": {
    "build": "node_modules/.bin/browserify src/app.js -o app.js -t [ babelify --presets [ latest react ] ]",
    "serve": "node_modules/.bin/http-server",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "babel-preset-latest": "^6.14.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "react": "^15.3.1",
    "react-dom": "^15.3.1",
    "react-music": "^1.0.1"
  },
  "dependencies": {
    "http-server": "^0.9.0"
  }
}
